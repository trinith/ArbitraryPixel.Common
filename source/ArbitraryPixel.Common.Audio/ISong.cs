namespace ArbitraryPixel.Common.Audio
{
    /// <summary>
    /// Represents an object that contains data about a song that can be played in the background.
    /// </summary>
    public interface ISong : IWrapper
    {
        // Currently don't need anything.
    }
}
